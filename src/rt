#!/bin/sh
LOGFILE=${HOME}/Mail/RootMail
root-tail -f -geometry 1400x700-64-64 ${LOGFILE},gold2,
echo -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- >>${LOGFILE}
